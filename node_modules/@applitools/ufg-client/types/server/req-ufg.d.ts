import type { Proxy } from '@applitools/types';
import { type Logger } from '@applitools/logger';
import { type Req, type Options } from '@applitools/req';
export declare type ReqUFGConfig = {
    serverUrl: string;
    accessToken: string;
    proxy?: Proxy;
    agentId?: string;
    connectionTimeout?: number;
};
export declare type ReqUFGOptions = Options & {
    name: string;
    expected?: number | number[];
    logger?: Logger;
};
export declare type ReqUFG = Req<ReqUFGOptions>;
export declare function makeReqUFG({ config, logger }: {
    config: ReqUFGConfig;
    logger: Logger;
}): Req<ReqUFGOptions>;
