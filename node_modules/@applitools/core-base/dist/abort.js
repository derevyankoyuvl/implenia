"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.makeAbort = void 0;
function makeAbort({ requests, logger: defaultLogger }) {
    let results;
    return async function abort({ logger = defaultLogger } = {}) {
        logger.log('Command "abort" is called');
        results !== null && results !== void 0 ? results : (results = await requests.abort({ logger }));
        return results;
    };
}
exports.makeAbort = makeAbort;
