import type { TextRegion } from '@applitools/types';
import type { Target, LocateTextSettings } from '@applitools/types/base';
import { type Logger } from '@applitools/logger';
import { type EyesRequests } from './server/requests';
declare type Options = {
    requests: EyesRequests;
    logger: Logger;
};
export declare function makeLocateText({ requests, logger: defaultLogger }: Options): <TPattern extends string>({ target, settings, logger, }: {
    target: Target;
    settings?: LocateTextSettings<TPattern, import("@applitools/types").Region>;
    logger?: Logger;
}) => Promise<Record<TPattern, TextRegion[]>>;
export {};
